import React from "react";
import { Row, Col } from "react-bootstrap";
import CandidateCard from "../Positions/candidateCard";
const ContainerPositions = (props) => {
  console.log(props);
  return <Row></Row>;
};

export default ContainerPositions;
